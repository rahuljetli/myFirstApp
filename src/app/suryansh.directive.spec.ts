import { SuryanshDirective } from './suryansh.directive';

describe('SuryanshDirective', () => {
  it('should create an instance', () => {
    const directive = new SuryanshDirective();
    expect(directive).toBeTruthy();
  });
});
